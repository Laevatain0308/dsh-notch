# Notch 动作与状态转场

本次范围：运行转为审核、成功或失败；分别覆盖只有一个任务，以及两个任务中一个改变状态。保留用户最近同步的玻璃外壳、排版和机器人资源；候选效果先在独立原生预览中验收，不安装到 DSH。

## 动效规则

由当前蓝色弧线出发，头部沿路径前进、尾部跟随，途中连续变色。黄色审核和绿色成功向上，红色失败向下。到达目标后沿圆周画线，再出现实心圆及符号。有其他任务运行时，线条从目标色连续回到蓝色运行弧；最后一个任务结束或等待审核时，笔画收在目标圈，源槽随之收起。不得用整圈平移或同时淡入两个圆代替笔画移动。

沿用已接受的 0.95 秒笔画时间轴和 19 pt 圆直径，颜色、字形与坐标使用现有定义。自动展开审核面板等待笔画结束，主动交互不因动画被禁用；重复轮询不重播，旧回调不能覆盖新状态。减少动态效果直接显示最终状态。

## 设计依据与验证

| Active concept | 决策 | 实现位置 | 验证 |
| --- | --- | --- | --- |
| motion-language | 三种结果共用有方向的笔画语义；保留当前弧线作为起点 | StatusFlight / StatusOrbitView | 原速六场景原生预览与逐帧截图 |
| motion-contract | 同一时间轴约束路程、颜色、填充、布局；只在有剩余工作时返回 | BoardModel / OrbitLayout / OrbitMotionFrame | OutcomeProbe、MotionProbe、取消和重复事件检查 |

视觉沿用纯黑紧凑外壳、细线与圆角字形；本轮不增加装饰。主要识别特征是线条本身连续转移。原生 AppKit/SwiftUI 使用原生渲染证据，浏览器 motion validator 不适用。

## 本轮验证

黄色已使用 StatusFlight 的 decision 结果类别，沿红绿共享的路径、时间轴和填充顺序绘制。单任务中源数字与黑底在线条离开时淡出，避免收缩时压住结果圆。自动展开推迟到审核笔画完成，取消审核会取消待展开请求。

OutcomeProbe 六场景与重复事件、快速取消、连续结果检查通过；项目原 MotionProbe（含减少动态效果静止检查）通过。原生渲染器采样六场景的逐帧图像并人工检查，证据位于 `本地 motion proof 输出目录`。六场景本地预览已展示；运行中 DSH 未安装此候选，用户对自然程度的验收仍以预览反馈为准。

## 决策回复回程（本轮）

覆盖完整的 `蓝 1 → 黄 → 蓝 1` 与 `蓝 2 → 蓝 1＋黄 → 蓝 2`。单灯保留已接受的清空填充、从顶部画蓝环的回程；双灯由黄圈抽出笔画，沿既有路径的返回段汇入蓝环，再把数字从 1 渐变到 2。黄灯、线条与外壳使用同一进度，源填充消失后才收起槽位。若面板正在折叠，回程从折叠完成时开始，使线条可见。回复很快时接在去程后播放；取消且没有恢复运行时不制造蓝灯；轮询不重播，旧回调不覆盖新回程。

Active motion-language 绑定：回复恢复同一任务的可见因果 → DecisionReturn / StatusOrbitView → 两条往返原速预览。Active motion-contract 绑定：数字在汇入时更新、路径及旋转速度连续、折叠不吞动画 → BoardModel / DecisionReturnFrame → OutcomeProbe 和确定时间逐帧渲染。沿用当前颜色、19 pt 灯、0.95–0.98 秒时间轴、系统减少动态效果设置。

## 双语录屏

录屏标题使用中文在上、英文在下，只呈现同一个转换名称。六格与单景均保留固定标题区，英文不挤占动画空间。36 个场景由同一份生产源构建，录屏工具仅替换传输层并发送定向待机动作，不重写状态曲线。
